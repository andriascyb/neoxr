const { renderRegisterPage } = require('./pages/register');
const { renderLoginPage } = require('./pages/login');
const { renderDashboardPage } = require('./pages/dashboard');

module.exports = {
  renderRegisterPage,
  renderLoginPage,
  renderDashboardPage
};
