const { renderHero }       = require('./hero');
const { renderCard }       = require('./card');
const { renderNotifyScript } = require('./notify');
const { renderPageHeader } = require('./page-header');

module.exports = {
  renderHero,
  renderCard,
  renderNotifyScript,
  renderPageHeader
};
