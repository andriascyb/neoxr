const { renderHero } = require('./hero');
const { renderCard } = require('./card');
const { renderNotifyScript } = require('./notify');

module.exports = {
  renderHero,
  renderCard,
  renderNotifyScript
};
